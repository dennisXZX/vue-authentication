## Vue Authentication and Validation

Powered by Vue, Vuex, Vuelidate, Axios, Firebase

#### Steps
- run `npm install` to install dependencies
- run `npm run dev` to run the Vue app
- default port: `localhost:8080`